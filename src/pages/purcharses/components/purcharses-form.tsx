import { DialogContent, DialogHeader, } from "@/components/ui/dialog";

export function PurcharsesForm() {
  return (
    <DialogContent>
      <DialogHeader>Compras</DialogHeader>
    </DialogContent>
  );
}
